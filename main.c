#include <stdio.h>
#include <stdlib.h>

int main()
{
    int menu, subMenuBic;
    switch(menu){
        case '1':
            do{
                switch(subMenuBic){

                }
            }while(subMenuBic != 0);
    }

    return 0;
}
